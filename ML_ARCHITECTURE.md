# 🏗️ ML & Vision Architecture

## Complete System Diagram

```
USER BROWSER (localhost:3000)
         │
         │ User uploads text + image
         ▼
┌────────────────────────────┐
│   REACT FRONTEND (Vite)    │
│   • Login/Signup Pages     │
│   • Input Page             │
│   • Results Page           │
└────────┬───────────────────┘
         │
         │ POST /api/analyze
         │ {text: "...", image: "base64..."}
         ▼
┌─────────────────────────────────────────┐
│  FLASK BACKEND (localhost:5000)         │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │   TEXT PIPELINE                 │   │
│  │                                 │   │
│  │   Hugging Face Transformers     │   │
│  │   ┌────────────────────────┐    │   │
│  │   │ DistilBERT             │    │   │
│  │   │ Sentiment Analysis     │    │   │
│  │   │ → POSITIVE/NEGATIVE    │    │   │
│  │   └────────────────────────┘    │   │
│  │   ┌────────────────────────┐    │   │
│  │   │ GPT-2                  │    │   │
│  │   │ Caption Generation     │    │   │
│  │   │ → 9 platform captions  │    │   │
│  │   └────────────────────────┘    │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │   IMAGE PIPELINE                │   │
│  │                                 │   │
│  │   OpenCV + CNNs                 │   │
│  │   ┌────────────────────────┐    │   │
│  │   │ Haar Cascade           │    │   │
│  │   │ Face Detection         │    │   │
│  │   │ → Count: 2 faces       │    │   │
│  │   └────────────────────────┘    │   │
│  │   ┌────────────────────────┐    │   │
│  │   │ ResNet-50 CNN          │    │   │
│  │   │ Image Classification   │    │   │
│  │   │ → Top 3 predictions    │    │   │
│  │   └────────────────────────┘    │   │
│  │   ┌────────────────────────┐    │   │
│  │   │ OpenCV Analysis        │    │   │
│  │   │ • Edge Detection       │    │   │
│  │   │ • Color Analysis       │    │   │
│  │   │ • Brightness           │    │   │
│  │   └────────────────────────┘    │   │
│  └─────────────────────────────────┘   │
│                                         │
│          RESULT AGGREGATION             │
│          → JSON Response                │
└────────┬────────────────────────────────┘
         │
         │ Returns analysis results
         ▼
┌────────────────────────────┐
│   RESULTS PAGE             │
│   • Decision: APPROVED     │
│   • 9 AI Captions          │
│   • 45 Smart Hashtags      │
│   • ML Insights            │
└────────────────────────────┘
```

## Models Used

### 🤖 Hugging Face Transformers
1. **DistilBERT** - Sentiment Analysis
2. **GPT-2** - Caption Generation

### 👁️ Computer Vision
1. **ResNet-50** - Image Classification (PyTorch)
2. **Haar Cascades** - Face Detection (OpenCV)

## Processing Flow

```
INPUT: Text + Image
    ↓
TEXT → DistilBERT → Sentiment (POSITIVE/NEGATIVE)
    ↓
TEXT → GPT-2 → Captions (Instagram, Facebook, LinkedIn)
    ↓
IMAGE → Decode base64 → NumPy array
    ↓
IMAGE → Haar Cascade → Face count
    ↓
IMAGE → ResNet-50 → Classification
    ↓
IMAGE → OpenCV → Edge/Color/Brightness
    ↓
COMBINE → Final JSON Response
    ↓
DISPLAY in Results Page
```

## Performance

**CPU:** 5-8 seconds per analysis
**GPU:** 1-2 seconds per analysis

Models cached after first load!
