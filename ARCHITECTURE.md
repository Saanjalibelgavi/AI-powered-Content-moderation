# Component Architecture

## 📐 Application Structure

```
┌─────────────────────────────────────────────────────────────┐
│                         App.jsx                              │
│                    (Root Component)                          │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                     Dashboard.jsx                            │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  State Management:                                    │  │
│  │  • uploadedImage                                      │  │
│  │  • imagePreview                                       │  │
│  │  • isAnalyzing                                        │  │
│  │  • contentSuggestions                                 │  │
│  │  • moderationResult                                   │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌─────────────────────┐  ┌────────────────────────────┐   │
│  │                     │  │                            │   │
│  │  ImageUpload.jsx    │  │   Results Section          │   │
│  │                     │  │   ┌────────────────────┐   │   │
│  │  ┌───────────────┐  │  │   │                    │   │   │
│  │  │ Upload Zone   │  │  │   │ ModerationResult   │   │   │
│  │  │ - Drag & Drop │  │  │   │ .jsx               │   │   │
│  │  │ - File Input  │  │  │   │                    │   │   │
│  │  └───────────────┘  │  │   │ • Status Card      │   │   │
│  │         │            │  │   │ • Confidence Bar   │   │   │
│  │         ▼            │  │   │ • Flags Display    │   │   │
│  │  ┌───────────────┐  │  │   │ • Categories       │   │   │
│  │  │ Image Preview │  │  │   │                    │   │   │
│  │  │ + Remove Btn  │  │  │   └────────────────────┘   │   │
│  │  └───────────────┘  │  │            │               │   │
│  │         │            │  │            ▼               │   │
│  │         ▼            │  │   ┌────────────────────┐   │   │
│  │  ┌───────────────┐  │  │   │                    │   │   │
│  │  │ Analyze Btn   │  │  │   │ ContentSuggestions │   │   │
│  │  │ (with spinner)│  │  │   │ .jsx               │   │   │
│  │  └───────────────┘  │  │   │                    │   │   │
│  │                     │  │   │ • Tone Selector    │   │   │
│  └─────────────────────┘  │   │ • Captions List    │   │   │
│                            │   │ • Hashtags List    │   │   │
│                            │   │ • Copy Buttons     │   │   │
│                            │   │                    │   │   │
│                            │   └────────────────────┘   │   │
│                            │                            │   │
│                            └────────────────────────────┘   │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

```
User Action
    │
    ▼
┌─────────────────┐
│ Upload Image    │
└────────┬────────┘
         │
         ▼
┌─────────────────────┐
│ ImageUpload         │
│ • Validates file    │
│ • Creates preview   │
│ • Calls callback    │
└────────┬────────────┘
         │
         ▼
┌─────────────────────────┐
│ Dashboard               │
│ • Sets uploadedImage    │
│ • Sets imagePreview     │
│ • Resets old results    │
└────────┬────────────────┘
         │
         │ User clicks "Analyze"
         ▼
┌─────────────────────────┐
│ handleAnalyzeContent()  │
│ • Sets isAnalyzing=true │
│ • Calls AI service      │
│ (currently mock data)   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ AI Service / Mock API   │
│ • Processes image       │
│ • Returns moderation    │
│ • Returns suggestions   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Dashboard State Update  │
│ • moderationResult      │
│ • contentSuggestions    │
│ • isAnalyzing=false     │
└────────┬────────────────┘
         │
         ├───────────────┐
         │               │
         ▼               ▼
┌──────────────┐  ┌─────────────────┐
│ Moderation   │  │ Content         │
│ Result       │  │ Suggestions     │
│              │  │                 │
│ • Display    │  │ • Display       │
│   status     │  │   captions      │
│ • Show       │  │ • Show          │
│   confidence │  │   hashtags      │
│ • List flags │  │ • Enable copy   │
└──────────────┘  └─────────────────┘
```

## 🎨 Styling Architecture

```
index.css (Global)
├── CSS Variables (:root)
│   ├── Colors (primary, secondary, success, danger, etc.)
│   ├── Backgrounds (dark theme)
│   ├── Text colors
│   └── Shadows & effects
├── Reset styles
├── Body defaults
└── Utility classes

App.css (Minimal)
└── App container

Dashboard.css
├── Dashboard layout (grid)
├── Header & logo
├── Section containers
├── Analyze button
└── Empty states

ImageUpload.css
├── Upload zone (drag-drop area)
├── Image preview
├── Remove button
└── Responsive adjustments

ContentSuggestions.css
├── Tone selector
├── Suggestions groups
├── Suggestion items
├── Copy buttons
└── Mobile layout

ModerationResult.css
├── Status card (approved/rejected)
├── Confidence bar
├── Flags section
├── Categories badges
└── Icons & colors
```

## 📦 Services Layer

```
aiService.js
│
├── Configuration
│   └── AI_API_CONFIG (base URL, timeout)
│
├── Functions
│   ├── analyzeImageContent(imageFile)
│   │   └── POST /moderate
│   │
│   ├── generateContentSuggestions(imageFile, tone)
│   │   └── POST /generate-content
│   │
│   └── analyzeAndGenerate(imageFile, tone)
│       └── POST /analyze-full (recommended)
│
└── Error Handling
    ├── Network errors
    ├── API errors
    └── Validation errors
```

## 🔌 Integration Points

```
┌────────────────────┐
│   React Frontend   │
│  (Current Project) │
└─────────┬──────────┘
          │
          │ HTTP/HTTPS
          │ (axios)
          ▼
┌────────────────────┐
│   Backend API      │
│  (To be created)   │
└─────────┬──────────┘
          │
          │ API Calls
          ▼
┌────────────────────┐
│   AI Services      │
│                    │
│  ├─ OpenAI         │
│  ├─ Google Vision  │
│  ├─ Azure CV       │
│  └─ AWS Rekognition│
└────────────────────┘
```

## 🛠️ Component Props

### ImageUpload
```javascript
Props:
  onImageUpload: (file: File) => void
  imagePreview: string | null
  onReset: () => void
```

### ContentSuggestions
```javascript
Props:
  suggestions: {
    captions: string[]
    hashtags: string[]
    tones: string[]
  }
```

### ModerationResult
```javascript
Props:
  result: {
    isApproved: boolean
    confidence: number
    flags: {
      inappropriate: boolean
      violence: boolean
      spam: boolean
      adult: boolean
    }
    categories: string[]
  }
```

## 🎯 Event Flow

```
┌────────────┐
│  File Drop │
│  / Select  │
└──────┬─────┘
       │
       ▼
┌────────────────┐
│ Validate File  │
│ (type, size)   │
└──────┬─────────┘
       │
       ▼
┌────────────────┐
│ Create Preview │
│ (FileReader)   │
└──────┬─────────┘
       │
       ▼
┌────────────────┐
│ Update State   │
│ (Dashboard)    │
└──────┬─────────┘
       │
       │ User clicks Analyze
       ▼
┌────────────────┐
│ API Call       │
│ (2s simulated) │
└──────┬─────────┘
       │
       ▼
┌────────────────┐
│ Process Result │
│ (moderation +  │
│  suggestions)  │
└──────┬─────────┘
       │
       ▼
┌────────────────┐
│ Render Results │
│ (conditional)  │
└────────────────┘
```

## 📱 Responsive Behavior

```
Desktop (>1024px)
┌─────────────────────────────────┐
│         Header (full)           │
├──────────────┬──────────────────┤
│              │                  │
│   Upload     │    Results       │
│   Section    │    Section       │
│   (50%)      │    (50%)         │
│              │                  │
└──────────────┴──────────────────┘

Tablet (768px-1024px)
┌─────────────────────────────────┐
│         Header (full)           │
├─────────────────────────────────┤
│         Upload Section          │
│           (100%)                │
├─────────────────────────────────┤
│        Results Section          │
│           (100%)                │
└─────────────────────────────────┘

Mobile (<768px)
┌──────────────┐
│ Header (sm)  │
├──────────────┤
│    Upload    │
│   Section    │
│   (stacked)  │
├──────────────┤
│   Results    │
│   Section    │
│   (stacked)  │
└──────────────┘
```

## 🧩 Reusable Patterns

### Button Pattern
```javascript
<button className="action-button" onClick={handler}>
  <Icon size={20} />
  Button Text
</button>
```

### Card Pattern
```javascript
<div className="card">
  <h3 className="card-title">
    <Icon size={24} />
    Title
  </h3>
  <div className="card-content">
    {content}
  </div>
</div>
```

### List Item Pattern
```javascript
<div className="list-item">
  <p className="item-text">{text}</p>
  <button className="item-action">
    <Icon size={16} />
    Action
  </button>
</div>
```

---

This architecture provides:
- 🎯 **Clear separation** of concerns
- 🔄 **Unidirectional data** flow
- 📦 **Modular components**
- 🎨 **Consistent styling**
- 🔌 **Easy integration**
- 📱 **Responsive design**
